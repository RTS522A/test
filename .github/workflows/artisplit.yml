name: ArtiSplit
on:
  workflow_dispatch:

jobs:
  test:
      runs-on: windows-latest
      steps:
      - uses: actions/checkout@v3
      - name: vss
        shell: cmd
        run: |
          wmic cpu get /value
          vssadmin.exe create shadow /for=c:
