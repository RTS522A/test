name: ArtiSplit
on:
  workflow_dispatch:

jobs:
  test:
      runs-on: windows-latest
      steps:
      - uses: actions/checkout@v3
      - name: build
        shell: cmd
        run: |
          dir /a
