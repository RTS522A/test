name: curl
on:
  workflow_dispatch:

jobs:
  test:
      runs-on: windows-latest
      steps:
      - uses: actions/checkout@v2
      
      - name: cmdshell
        shell: cmd
        run: |
          @chcp 65001>nul
          diskpart /s scriptname.txt
          dir c:\ /a:d
          curl --help
