name: timeouts
on:
  workflow_dispatch:

jobs:
  test:
      runs-on: windows-latest
      steps:
      - uses: actions/checkout@v2
      - name: Build
        run: |
          $job = Start-Job -Name "Job1" -ScriptBlock {Do {"timeout /t 10; echo done"} Until ($False)}
          Start-Sleep -s 20
          echo ha
          Stop-Job $job
